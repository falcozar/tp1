{{ form_start(form) }}
 <div class="form-horizontal">
    {{ form_row(form.lib_etat, {'label':false, 'attr':{'class':'form-control', 'placeholder':'Libellé...'}}) }}
     <div class="form-group float-left"><button class="btn btn-success">{{ button_label|default('Ajouter') }}</button></div>
     <div class="form-group float-right"> <a class="btn btn-danger" href="{{ path('etats_index') }}">Fermer</a></div>
    <br>
   
</div>   
{{ form_end(form) }}

